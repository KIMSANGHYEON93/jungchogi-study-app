// AI 엔드포인트의 입력 상한 — **서버와 브라우저가 함께 쓰는 단 하나의 출처.**
//
// 이 파일은 의존성이 없다(`node:*` 도 import 하지 않는다). 그래야 서버(`lib/ai/guard.js`)와
// 브라우저 번들(`src/services/aiClient.js`)이 같은 값을 import 할 수 있다.
// `lib/ai/guard.js` 는 `node:crypto` 를 쓰므로 브라우저에서 import 할 수 없어,
// 상수를 여기로 뺐다. **여기에 무언가를 import 하는 순간 그 전제가 깨진다.**

/**
 * 사용자가 쓴 답안의 최대 길이(문자 수).
 *
 * 서버는 이 값을 넘으면 400 으로 거절하고, 클라이언트는 보내기 전에 잘라
 * 잘렸다는 사실을 호출부에 알린다. 두 쪽이 다른 값을 보면 사용자는
 * "왜 잘렸는지" 또는 "왜 400 인지"를 알 수 없다.
 */
export const MAX_USER_ANSWER_LENGTH = 2_000;
